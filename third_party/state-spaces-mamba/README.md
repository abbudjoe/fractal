This path is the repo-owned default checkout location for optional Mamba-3 wiring checks.

Place a checkout of the official `state-spaces/mamba` repository here if you want the ignored
`official-module-wiring-*` tests to run without setting `FRACTAL_MAMBA3_OFFICIAL_REPO`.
